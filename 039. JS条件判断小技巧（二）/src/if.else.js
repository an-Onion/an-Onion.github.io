function greeting(role, access) {
  if( 'owner' ===  role ){
    if( 'public' === access ){

    }
    if( 'inside' === access ){

    }
  } else if ( 'admin' ===  role ){
    if( 'public' === access ){

    }
    if( 'inside' === access ){

    }
  } else if( 'hr' === role ) {

  }
}
